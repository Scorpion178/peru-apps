import { ImagedefaultPipe } from './imagedefault.pipe';

describe('ImagedefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagedefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
